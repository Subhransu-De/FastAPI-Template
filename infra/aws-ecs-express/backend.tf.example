terraform {
  backend "s3" {
    bucket       = "replace-with-your-terraform-state-bucket"
    key          = "fastapi-template/ecs-express/terraform.tfstate"
    region       = "ap-south-1"
    encrypt      = true
    use_lockfile = true
  }
}
