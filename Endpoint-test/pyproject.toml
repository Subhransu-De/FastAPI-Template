[project]
name = "endpoint-test"
version = "0.1.0"
description = "Standalone Behave endpoint tests for the FastAPI template application."
requires-python = ">=3.13"
dependencies = [
    "behave>=1.3.3",
    "httpx>=0.28.1",
]

[tool.uv]
package = false
